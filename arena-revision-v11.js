/* 9. SINIF ARENA V12 — Ders tekrarı süre doğrulama + ARENA soru merkezi */
(()=>{
  const todayKey=()=>{const d=new Date();return [d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-')};
  let lessonTick=null,lessonBlockSec=0,promptOpen=false,promptTimer=null,promptSec=15,saveTick=0;
  function ensureToday(){const t=todayKey();if(state.lessonReviewDate!==t){state.lessonReviewDate=t;state.lessonReviewSecondsToday=0;state.lessonReviewConfirmedDate='';state.lessonReviewPromptCount=0;state.lessonReviewAutoReturnCount=0;save()}}
  function saveLessonTime(){try{save()}catch(e){}}
  function closePrompt(){promptOpen=false;clearInterval(promptTimer);promptTimer=null;document.getElementById('lessonContinuePrompt')?.classList.remove('show')}
  function goHomeFromPrompt(){ensureToday();state.lessonReviewAutoReturnCount=Number(state.lessonReviewAutoReturnCount||0)+1;saveLessonTime();closePrompt();lessonBlockSec=0;oldGo('home')}
  function confirmContinue(){ensureToday();state.lessonReviewConfirmedDate=todayKey();state.lessonReviewPromptCount=Number(state.lessonReviewPromptCount||0)+1;saveLessonTime();closePrompt();lessonBlockSec=0}
  function ensurePrompt(){let el=document.getElementById('lessonContinuePrompt');if(el)return el;el=document.createElement('div');el.id='lessonContinuePrompt';el.className='lesson-check';el.innerHTML=`<section class="lesson-check-card"><small>DERS TEKRARI KONTROLÜ</small><h3>Devam ediyor musun?</h3><p>Bu bölümde 3 dakikadır bulunuyorsun. Ders tekrarına devam ediyorsan onayla.</p><div class="lesson-check-count"><b id="lessonCheckSec">15</b><span>saniye</span></div><button id="lessonCheckYes">EVET, DEVAM EDİYORUM</button><button id="lessonCheckHome">ANA SAYFAYA DÖN</button><em>Yanıt verilmezse otomatik olarak ana sayfaya dönülür.</em></section>`;document.body.appendChild(el);document.getElementById('lessonCheckYes').onclick=confirmContinue;document.getElementById('lessonCheckHome').onclick=goHomeFromPrompt;return el}
  function askContinue(){if(promptOpen||view!=='lessons')return;promptOpen=true;promptSec=15;const el=ensurePrompt();el.classList.add('show');document.getElementById('lessonCheckSec').textContent=promptSec;clearInterval(promptTimer);promptTimer=setInterval(()=>{promptSec--;const n=document.getElementById('lessonCheckSec');if(n)n.textContent=Math.max(0,promptSec);if(promptSec<=0)goHomeFromPrompt()},1000)}
  function startLessonClock(){ensureToday();if(lessonTick)return;lessonTick=setInterval(()=>{if(view!=='lessons'||document.visibilityState!=='visible'||promptOpen)return;ensureToday();state.lessonReviewSecondsToday=Number(state.lessonReviewSecondsToday||0)+1;lessonBlockSec++;saveTick++;if(saveTick>=10){saveTick=0;saveLessonTime()}if(lessonBlockSec>=180)askContinue()},1000)}
  function stopLessonClock(){if(lessonTick){clearInterval(lessonTick);lessonTick=null}saveLessonTime()}
  const oldGo=go;go=function(v){if(view==='lessons'&&v!=='lessons')stopLessonClock();oldGo(v);if(v==='lessons'){lessonBlockSec=0;startLessonClock()}};
  document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='hidden')saveLessonTime()});window.addEventListener('pagehide',saveLessonTime);
  buildLessons=function(){const root=$('lessonGrid');if(!root)return;root.className='lesson-summary-list';root.innerHTML=subjects.map(subject=>{const items=notes[subject]||[];return `<section class="lesson-summary-card card"><header class="lesson-summary-head"><span class="lesson-summary-icon">${icons[subject]||'📘'}</span><div><small>DERS TEKRARI</small><h3>${short[subject]||subject}</h3></div></header><div class="unit-list">${items.map((n,i)=>`<details class="unit-item" ${i===0?'open':''}><summary><span>${i+1}. ÜNİTE</span><b>${n[0]}</b></summary><div class="unit-body"><h4>Konu Özeti</h4><p>${n[1]}</p><h4>Temel Kavramlar</h4><p>${n[2]}</p><h4>Örnek / Uygulama</h4><p>${n[3]}</p><div class="unit-caution"><b>Dikkat</b><p>${n[4]}</p></div></div></details>`).join('')}</div></section>`}).join('')};
  function buildArenaCenter(){const root=document.querySelector('#exams .exam-grid');if(!root)return;root.className='arena-question-center';root.innerHTML=`<section class="card arena-question-hero"><small>ARENA SORU MERKEZİ</small><h3>Sadece soru çözümü</h3><p>Serbest çalışma, ders bazlı çalışma ve deneme modlarından birini seç.</p><button onclick="startStudy()">🧠 SERBEST SORU ÇÖZ</button></section><section class="card arena-mode-card"><h3>📚 DERS SEÇEREK ÇÖZ</h3><div class="arena-subject-buttons">${subjects.map(s=>`<button onclick='startSubject(${JSON.stringify(s)})'><span>${icons[s]||'•'}</span>${short[s]||s}</button>`).join('')}</div></section><section class="card arena-mode-card"><h3>🏁 DENEME MODU</h3><p>Karışık derslerden 30 soruluk Arena denemesi.</p><button class="arena-primary" onclick="startExam()">30 SORULUK DENEMEYİ BAŞLAT</button></section>`}
  const oldRender=render;render=function(){oldRender();buildArenaCenter()};
  const lessonTitle=document.querySelector('#lessons .view-title');if(lessonTitle)lessonTitle.innerHTML='<h2>Ders Tekrarı</h2><small>Üniteler • konular • geniş özetler</small>';
  const examTitle=document.querySelector('#exams .view-title');if(examTitle)examTitle.innerHTML='<h2>ARENA</h2><small>Soru çözüm merkezi</small>';
  const examNav=document.querySelector('.bottom-nav button[data-view="exams"] span');if(examNav)examNav.textContent='ARENA';
  const homeArena=[...document.querySelectorAll('.quick-grid button')].find(b=>b.getAttribute('onclick')?.includes("go('exams')"));if(homeArena){const b=homeArena.querySelector('b'),s=homeArena.querySelector('small');if(b)b.textContent='ARENA';if(s)s.textContent='Soru çözüm merkezi'}
  if(view==='lessons'){lessonBlockSec=0;startLessonClock()}render();
})();